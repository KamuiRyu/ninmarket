{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently \"npm run laravel\" \"npm run react\"",
        "laravel": "cd ./web && php artisan serve",
        "react": "cd ./app && npm start"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "concurrently": "^8.2.0"
    }
}
